/**
 * clr router
 */

// Empty default router -- custom route files below are the single source
// of truth for both public and authenticated routes (same convention as
// api/credential/routes/credential.ts).
export default {
  routes: []
}
